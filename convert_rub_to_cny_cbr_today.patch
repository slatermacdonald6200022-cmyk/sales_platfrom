diff --git a/uploads/processors/normalize_1c.py b/uploads/processors/normalize_1c.py
--- a/uploads/processors/normalize_1c.py
+++ b/uploads/processors/normalize_1c.py
@@ -1,5 +1,9 @@
+import datetime
 import os
 import re
+import urllib.request
+import xml.etree.ElementTree as ET
+
 import pandas as pd
 from openpyxl import load_workbook
 
@@ -29,11 +33,53 @@ def parse_number(val):
         return 0.0
 
 
-def normalize_1c_file(file_path):
+def get_current_cny_rate(rate_date=None):
+    """Возвращает официальный курс ЦБ: сколько RUB стоит 1 CNY."""
+    rate_date = rate_date or datetime.date.today()
+    date_req = rate_date.strftime('%d/%m/%Y')
+    url = f'https://www.cbr.ru/scripts/XML_daily.asp?date_req={date_req}'
+    request = urllib.request.Request(
+        url,
+        headers={'User-Agent': 'sales-platform/1.0'}
+    )
+
+    try:
+        with urllib.request.urlopen(request, timeout=10) as response:
+            root = ET.fromstring(response.read())
+
+        for valute in root.findall('Valute'):
+            if valute.findtext('CharCode') == 'CNY':
+                nominal = int(valute.findtext('Nominal'))
+                value = float(valute.findtext('Value').replace(',', '.'))
+                rate = value / nominal
+                if rate <= 0:
+                    break
+                return rate
+    except Exception as exc:
+        raise RuntimeError(
+            f'Не удалось получить курс CNY ЦБ РФ на {date_req}. '
+            'Загрузка остановлена, чтобы не записать рубли как юани.'
+        ) from exc
+
+    raise RuntimeError(
+        f'В ответе ЦБ РФ нет корректного курса CNY на {date_req}. '
+        'Загрузка остановлена, чтобы не записать рубли как юани.'
+    )
+
+
+def normalize_1c_file(file_path, cny_rate=None):
     """Парсит отчет валовой прибыли 1C ERP и возвращает плоскую таблицу."""
     if not file_path or not os.path.exists(file_path):
         return pd.DataFrame()
 
+    # Курс запрашивается один раз на дату загрузки и применяется ко всему файлу.
+    # В выгрузке 1С выручка указана в рублях; для CNY нужно делить RUB на RUB/CNY.
+    if cny_rate is None:
+        cny_rate = get_current_cny_rate()
+    cny_rate = float(cny_rate)
+    if cny_rate <= 0:
+        raise ValueError('Курс CNY должен быть больше нуля.')
+
     df_raw = pd.read_excel(file_path, header=None)
 
     # 1. Поиск периода в шапке
@@ -135,8 +181,8 @@ def normalize_1c_file(file_path):
                 continue
 
         qty_val = parse_number(row.iloc[col_qty])
-        cny_val = parse_number(row.iloc[col_cny])
-        if qty_val == 0 and cny_val == 0:
+        rub_val = parse_number(row.iloc[col_cny])
+        if qty_val == 0 and rub_val == 0:
             continue
 
         records.append({
@@ -147,7 +193,7 @@ def normalize_1c_file(file_path):
             'Номер месяца': period_month,
             'Месяц': f"{period_year}-{period_month:02d}",
             'Факт, шт': qty_val,
-            'Факт, CNY': cny_val
+            'Факт, CNY': rub_val / cny_rate
         })
 
     workbook.close()
diff --git a/uploads/tests.py b/uploads/tests.py
--- a/uploads/tests.py
+++ b/uploads/tests.py
@@ -35,7 +35,7 @@ class Normalize1CFileTests(SimpleTestCase):
         try:
             workbook.save(path)
-            result = normalize_1c_file(path)
+            result = normalize_1c_file(path, cny_rate=10)
         finally:
             workbook.close()
             os.unlink(path)
 
@@ -46,6 +46,6 @@ class Normalize1CFileTests(SimpleTestCase):
         self.assertEqual(row["Год"], 2026)
         self.assertEqual(row["Номер месяца"], 5)
         self.assertEqual(row["Факт, шт"], 6)
-        self.assertEqual(row["Факт, CNY"], 600)
+        self.assertEqual(row["Факт, CNY"], 60)
 
 # Create your tests here.
             workbook.close()
             os.unlink(path)
+
 @@ -48,7 +48,7 @@ class Normalize1CFileTests(SimpleTestCase):
